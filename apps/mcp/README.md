# @citecheck/mcp

MCP server for automatic paper reference repair.

Quick start:

```bash
claude mcp add citecheck -- npx -y @citecheck/mcp
```

Local run:

```bash
npx tsx src/mcp.ts
```

Exposed tools:

- `repair_paper`
- `citecheck_version`
